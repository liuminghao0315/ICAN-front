# 前端配置说明

前端已移除 `.env` 文件，改用 TypeScript 配置文件。

## 配置文件位置
`frontend/src/config/index.ts`

## 配置项说明

### 1. API 基础地址 (apiBaseUrl)
后端 API 的基础地址。

**开发环境：**
```typescript
apiBaseUrl: 'http://localhost:8080'
```

**生产环境：**
```typescript
apiBaseUrl: 'https://api.yourdomain.com'
```

### 2. WebSocket 基础地址 (wsBaseUrl)
WebSocket 服务的基础地址，用于实时推送任务进度、反馈消息等。

**开发环境：**
```typescript
wsBaseUrl: 'ws://localhost:8080'
```

**生产环境：**
```typescript
wsBaseUrl: 'wss://api.yourdomain.com'
```

### 3. DeepSeek API 配置 (deepseek)
用于风险词库的 AI 智能生成功能。

```typescript
deepseek: {
  // DeepSeek API Key - 必填
  // 获取方式：访问 https://platform.deepseek.com/ 注册并获取 API Key
  apiKey: '填写你的 DeepSeek API Key',

  // 以下配置一般不需要修改
  apiUrl: 'https://api.deepseek.com/chat/completions',
  model: 'deepseek-chat',
}
```

**如何获取 DeepSeek API Key：**
1. 访问 https://platform.deepseek.com/
2. 注册/登录账号
3. 进入 API Keys 页面
4. 创建新的 API Key
5. 复制 API Key 并填入配置文件

### 4. Mock 模式 (mockMode)
是否启用 Mock 数据模式。

```typescript
mockMode: false  // false: 使用真实后端接口，true: 使用 mock 数据
```

### 5. 请求超时时间 (timeout)
HTTP 请求的超时时间（毫秒）。

```typescript
timeout: 30000  // 30秒
```

### 6. Token 刷新提前时间 (proactiveRefreshBeforeMs)
在 Access Token 过期前多久主动刷新（毫秒）。

```typescript
proactiveRefreshBeforeMs: 5 * 60 * 1000  // 5分钟
```

### 7. WebSocket 配置 (websocket)
WebSocket 连接的相关配置。

```typescript
websocket: {
  maxReconnect: 5,           // 最大重连次数
  reconnectInterval: 3000,   // 重连间隔（毫秒）
  heartbeatInterval: 30000,  // 心跳间隔（毫秒）
}
```

## 完整配置示例

```typescript
export const config = {
  apiBaseUrl: 'http://localhost:8080',
  wsBaseUrl: 'ws://localhost:8080',
  deepseek: {
    apiKey: '填写你的 DeepSeek API Key',
    apiUrl: 'https://api.deepseek.com/chat/completions',
    model: 'deepseek-chat',
  },
  mockMode: false,
  timeout: 30000,
  proactiveRefreshBeforeMs: 5 * 60 * 1000,
  websocket: {
    maxReconnect: 5,
    reconnectInterval: 3000,
    heartbeatInterval: 30000,
  },
} as const
```

## 注意事项

1. 修改配置后需要重启前端开发服务器
2. 生产环境部署时，记得修改 `apiBaseUrl` 和 `wsBaseUrl` 为线上地址
3. WebSocket 地址的协议：开发环境用 `ws://`，生产环境（HTTPS）用 `wss://`
4. DeepSeek API Key 是必填项，否则风险词库的 AI 智能生成功能无法使用
